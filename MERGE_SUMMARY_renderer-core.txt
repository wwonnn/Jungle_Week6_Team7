============================================================
  feature/renderer-core -> main  Merge Summary
  Date: 2026-03-22
============================================================

## Branch Overview
- 35 commits (non-merge), 70 files changed
- +12,685 / -890 lines
- Main contributor: renderer-core (rendering pipeline overhaul)

## What was added / changed

### Rendering Pipeline
- Shader splitting: monolithic ShaderW0.hlsl -> Common/Primitive/Gizmo/Outline/Overlay/Editor/ShaderFont.hlsl
- Multi-pass RenderBus system (Opaque, Translucent, Editor, Grid, Outline, DepthLess, Overlay)
- RenderCollector refactored from global GUObjectArray iteration to per-actor collection
- Renderer multi-pass execution with rasterizer state switching (Lit/Wireframe ViewMode)
- Constant buffer separation: Frame vs PerObject constants

### New Features
- LineBatcher: debug line/box drawing (AABB visualization)
- FontBatcher: SpriteFont-based text rendering via DirectXTK
- BillboardComponent: quad-based billboard rendering
- ShowFlags: per-feature visibility toggle (Gizmo, Grid, Primitives, BillboardText)
- Selection outline with stencil-based rendering
- X-Ray effect for selection outline in Wireframe mode
- Cursor overlay rendering (mouse interaction feedback)

### Dependencies
- DirectXTK library added (ThirdParty/DirectXTK/)
- FontAtlas.dds texture resource added

### Component Changes
- USphereComponent/UPlaneComponent: virtual UpdateWorldAABB() override for accurate AABB
- UPrimitiveComponent: GetRenderCommand signature changed (removed view/proj params)
- GizmoComponent: inner/outer gizmo rendering with depth stencil states

### D3D Device
- Additional rasterizer states (WireFrame, SolidNoCull, WireframeNoCull)
- Additional depth stencil states (GizmoInside, GizmoOutside, StencilWrite, StencilOutline)
- Additional blend states (AlphaBlend)
- Stencil buffer support

## Conflict Resolution (10 files)

### EditorEngine.h / .cpp
- Kept main's UEditorEngine architecture (inherits UEngine)
- Discarded renderer-core's FEditorEngine with manual member management
- renderer-core's old ResetCamera/CloseScene/NewScene/Release replaced by main's versions

### EditorControlWidget.cpp
- Kept main's PrimitiveActors spawn pattern
- Removed renderer-core's EPT_Quad/BillboardComponent spawn case (broken merge artifact)

### PrimitiveComponent.h
- Kept renderer-core's virtual UpdateWorldAABB() for subclass overrides (Sphere, Plane)
- Removed main's mutable AABB + const approach
- Moved UpdateWorldAABB() call from UpdateWorldMatrix() to Raycast() (avoids const violation)

### RenderTypes.h
- Kept both: dxgi1_5.h (main's ALLOW_TEARING) + CoreTypes.h (renderer-core's types)

### RenderCollector.cpp
- Kept main's World->GetActors() iteration (replaced GUObjectArray)
- Fixed UCamera* -> UCameraComponent* rename

### EngineLoop.cpp
- Discarded renderer-core's InitializeTiming/TickFrame (replaced by FTimer in main)

### Engine.h / .cpp
- Added FRenderCollector as UEngine member (was missing after conflict resolution)
- Fixed static FRenderCollector:: calls to member calls

### vcxproj / vcxproj.filters
- Added BillboardComponent.h/.cpp entries (renderer-core)
- Kept CameraComponent.h/.cpp (main's rename from Camera)
- Kept Timer.h/.cpp (main)

### imgui.ini
- Kept main's layout values (trivial)

## Known Warnings (pre-existing, from renderer-core)
- C4244: double->float in USphereComponent/UPlaneComponent AABB
- C4244: float->uint32 in RenderCollector FColor
- LNK4099: DirectXTK.pdb not found (release build PDB missing)
